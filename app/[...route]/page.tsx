import Portal from '../portal';
import PublicSite from '../public-site';
import {headers} from 'next/headers';
import {redirect,notFound} from 'next/navigation';
import {user} from '@/lib/server/auth';
export const dynamic='force-dynamic';
const pages:Record<string,string>={'admin':'Overview','admin/registrations':'Registrations','admin/players':'Players & families','admin/seasons':'Seasons','admin/payments':'Payments','admin/waivers':'Waivers','admin/reports':'Reports & exports','admin/users':'Users & permissions','admin/activity':'Activity log','admin/settings':'Settings','portal':'Dashboard','portal/family':'My family','portal/players':'My family','portal/register':'Available seasons','portal/seasons':'Available seasons','portal/registrations':'My registrations','portal/payments':'Payments & receipts','portal/account':'Account settings','portal/settings':'Account settings'};
export default async function Routed({params,searchParams}:any){const {route}=await params;const path=route.join('/');if(['home','seasons','login','signup','forgot-password','reset-password','contact','help','privacy','terms'].includes(path))return <PublicSite page={path}/>;if(route[0]==='seasons'&&route.length===2)return <PublicSite page="season" seasonId={route[1]}/>;if(path==='preview/admin')return <Portal initialPage="Overview" demoMode/>;if(path==='preview/portal')return <Portal initialPage="Dashboard" initialPortal demoMode/>;if(path.startsWith('admin')||path.startsWith('portal')){const h=await headers();const u=await user(new Request('https://internal.local',{headers:h}));if(!u){const query=await searchParams;redirect('/login?returnTo='+encodeURIComponent('/'+path+(query.mode==='waiver'?'?mode=waiver':'')));}if(path.startsWith('admin')&&!['admin','staff'].includes(u.role))redirect('/portal');}const registering=route[0]==='portal'&&route[1]==='register'&&route.length===3;if(!pages[path]&&!registering)notFound();return <Portal initialPage={registering?'Available seasons':pages[path]} initialPortal={path.startsWith('portal')} initialSeasonId={registering?route[2]:undefined}/>}
